'''
prefix max over x subarray
current ans
if new big guy added:
    only add to score if y has same value
else:
    add to score if y <= current max
sum over y:
y = new max or y <= prv max if y not at new max position
monotonic stack backwards, keep increasing sequence
range count <= x, where x is increasing and elements


'''
